def max_freq(s):
  max_char=0
  max_count=0

  for c in s:       # o(n)
     count=0
     for ch in s:     # o(n)
         if ch==c:
            count+=1
     if count>max_count:
        max_count=count
        max_char=c
  return max_char,max_count

 tc - o(n^2) 
 sc - o(1)
